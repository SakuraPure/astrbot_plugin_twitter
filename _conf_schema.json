{
  "basic": {
    "description": "基础设置",
    "type": "object",
    "hint": "twikit 账号登录（cookie 或账号密码）、代理与轮询等基础配置。",
    "items": {
      "twitter_twikit_cookies_path": {
        "description": "twikit cookie 文件路径",
        "type": "string",
        "hint": "如 data/twikit_cookies.json；留空则使用插件目录下 data/twikit_cookies.json。推荐用 twikit_login.py 一次性生成。",
        "default": "data/twikit_cookies.json"
      },
      "twitter_twikit_username": {
        "description": "twikit 登录用户名（可选）",
        "type": "string",
        "hint": "用于自动登录生成 cookie；已用 cookie 登录时留空即可",
        "default": ""
      },
      "twitter_twikit_email": {
        "description": "twikit 登录邮箱（可选）",
        "type": "string",
        "hint": "辅助验证用，可与用户名相同或留空",
        "default": ""
      },
      "twitter_twikit_password": {
        "description": "twikit 登录密码（可选，敏感）",
        "type": "string",
        "hint": "用于自动登录生成 cookie；已用 cookie 登录时留空即可",
        "default": ""
      },
      "twitter_proxy": {
        "description": "代理地址（留空则不使用代理）",
        "type": "string",
        "hint": "例如: http://127.0.0.1:7890 或 socks5://127.0.0.1:1080",
        "default": ""
      },
      "twitter_poll_interval": {
        "description": "推文轮询间隔（分钟）",
        "type": "int",
        "default": 5,
        "hint": "建议不低于 3 分钟，避免频繁请求"
      }
    }
  },
  "message_format": {
    "description": "消息格式",
    "type": "object",
    "hint": "控制推文推送的消息样式、合并转发行为、媒体质量及帖子链接。",
    "items": {
      "twitter_use_node": {
        "description": "单条推文使用合并转发消息发送",
        "type": "bool",
        "default": true
      },
      "twitter_no_text": {
        "description": "推文含媒体时不输出文字内容",
        "type": "bool",
        "default": false
      },
      "twitter_text_render_mode": {
        "description": "推文正文渲染方式",
        "type": "string",
        "default": "text",
        "options": ["text", "screenshot"],
        "hint": "text=普通文字推送；screenshot=使用 AstrBot html_render 渲染 X 官方暗色时间线风格截图"
      },
      "twitter_screenshot_theme": {
        "description": "截图模式主题",
        "type": "string",
        "default": "dark",
        "options": ["dark", "light"],
        "hint": "dark=黑色背景 X 风格；light=白色背景 X 风格，仅在截图模式下生效"
      },
      "twitter_image_quality": {
        "description": "推文图片质量",
        "type": "string",
        "default": "orig",
        "options": ["large", "orig"],
        "hint": "large=缩略图，orig=原图（默认）"
      },
      "twitter_video_max_size_mb": {
        "description": "视频直发大小上限（MB）",
        "type": "int",
        "default": 256,
        "hint": "发送视频前会探测文件大小；超过上限时改为发送说明和视频链接。默认=256"
      },
      "twitter_collective_forward": {
        "description": "轮询时汇总多条新推文为一条合并转发消息",
        "type": "bool",
        "default": false,
        "hint": "仅影响轮询推送。开启后会缓存本轮轮询期间获取到的新推文，轮询结束后统一打包发送；需同时开启“单条推文使用合并转发消息发送”，否则不会生效"
      },
      "twitter_collective_max_authors": {
        "description": "集体转发时单条消息包含的最大推主数",
        "type": "int",
        "default": 5,
        "hint": "超过此数量的推主将分组为多条合并转发消息；仅集体转发模式下生效"
      },
      "twitter_include_tweet_link": {
        "description": "推送消息附带帖子链接",
        "type": "bool",
        "default": true,
        "hint": "开启后消息末尾附带对应 X/Twitter 帖子链接；关闭后轮询推送、推特测试和链接识别解析出的推文消息都不附带帖子链接"
      }
    }
  },
  "content_filter": {
    "description": "内容过滤",
    "type": "object",
    "hint": "控制转帖推送、链接识别等内容策略。",
    "items": {
      "twitter_include_retweets": {
        "description": "轮询推送和推特测试时推送转帖",
        "type": "bool",
        "default": true,
        "hint": "开启后会推送转帖，并在消息中标明谁转发了谁；关闭后轮询跳过转帖，/推特测试会寻找最新的非转贴推文"
      },
      "twitter_deduplicate_retweets": {
        "description": "轮询推送时对转帖去重",
        "type": "bool",
        "default": false,
        "hint": "开启后，同一条原帖被多个订阅推主转发时，每个会话只推送第一次"
      },
      "twitter_link_recognition_enabled": {
        "description": "推文链接识别全局开关",
        "type": "bool",
        "default": true,
        "hint": "关闭后，聊天中出现 twitter.com / x.com 链接时不再自动解析推文内容"
      }
    }
  },
  "translation": {
    "description": "翻译设置",
    "type": "object",
    "hint": "配置推文内容的自动翻译行为，需至少一个可用的 LLM Provider。",
    "items": {
      "twitter_translate_enabled": {
        "description": "推文内容翻译开关",
        "type": "bool",
        "default": false,
        "hint": "开启后，推文正文将被翻译为目标语言"
      },
      "twitter_translate_target_lang": {
        "description": "翻译目标语言",
        "type": "string",
        "default": "简体中文",
        "hint": "推文内容将被翻译为此语言，如：简体中文、日语、英语"
      },
      "twitter_translate_provider_id": {
        "description": "翻译使用的 LLM Provider ID（留空则自动选择）",
        "type": "string",
        "default": "",
        "hint": "指定翻译用的 Provider ID，留空时按以下顺序自动选择：当前会话 Provider → 第一个可用 Provider"
      },
      "twitter_translate_custom_prompt_enabled": {
        "description": "启用自定义翻译提示词",
        "type": "bool",
        "default": false,
        "hint": "开启后将使用下方自定义的翻译提示词，而非内置默认提示词"
      },
      "twitter_translate_custom_prompt": {
        "description": "自定义翻译提示词",
        "type": "text",
        "editor_mode": true,
        "editor_language": "markdown",
        "default": "你是一个专业的翻译助手。请将用户提供的文本翻译为{target_lang}。\n规则：仅输出翻译结果，不要添加任何解释、前缀、注释或原文对照\n保持原文的语气和格式（如换行、表情符号等）",
        "hint": "自定义翻译 system prompt。支持变量：{target_lang} 会替换为翻译目标语言。"
      }
    }
  }
}
